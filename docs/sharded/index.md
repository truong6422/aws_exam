# Sharded Documents Index

Use this folder for large markdown documents that need chunked context loading.

## Conventions
- Keep source doc in `docs/`.
- Create shard folder `docs/sharded/<doc-name>/`.
- Add fragment files as `NN-section-title.md`.
- Update this index with links and short summaries.

## Shards
- (add links here)
